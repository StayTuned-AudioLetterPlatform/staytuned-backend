spring:
  jpa:
    open-in-view: false
    database: mysql
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
    show-sql: true
  session:
    store-type: jdbc
  profiles:
    include: oauth

